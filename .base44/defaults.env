# Base44 defaults — overridable by the platform-managed /run/base44/app.env.
# BASE44_APP_ID: identifier of this app in Base44 (from the editor URL).
# Routes the AI interview through Base44's built-in InvokeLLM integration
# (anonymous mode, metered against the app's Base44 credit quota).
BASE44_APP_ID=6aad72ca6aaf4cf49e932168
